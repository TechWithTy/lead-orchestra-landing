{
	"buildCommand": "pnpm run build",
	"installCommand": "corepack enable && corepack prepare pnpm@8.15.4 --activate && SKIP_HUSKY=1 pnpm install --no-frozen-lockfile --prefer-offline",
	"framework": "nextjs"
}
